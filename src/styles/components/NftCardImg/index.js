import { Box, styled } from "@mui/material";

const NftCardImg = styled(Box)(() => ({
    borderRadius: "10px",
    width: "100%",
    height: "100%",
    objectFit: "cover",
}));


export default NftCardImg;   