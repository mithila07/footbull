import { Box, styled } from "@mui/material";

const NftCard = styled(Box)(() => ({
    borderRadius: "10px",
    backgroundColor: "rgba(255, 255, 255, 0.1)",
    padding: "20px",
    aspectRatio: "4/5",
    display: "flex",
}));


export default NftCard;   