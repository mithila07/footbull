import React from "react";
import { Box, Container } from "@mui/material";



export default function NavbarMobile({ breakpoint }) {
    return (
        <Container maxWidth="xl">
            <Box sx={{bgcolor: '#cfe8fc'}} />
        </Container>
    );
}